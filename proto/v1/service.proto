syntax = "proto3";

/* UNCOMMENT YOU NEED */
package service_name; // like a go package name

import "google/protobuf/timestamp.proto";

service ServiceName { // Change name
  // Some rpc methods
  rpc Operation(OperationRequest) returns (OperationResponse) {}
}

message OperationRequest {
  // Some request fields
}

message OperationResponse {
  // Some response fields
}
/**/